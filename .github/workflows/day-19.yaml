name: day-19
on:
  push:
    paths:
      - 'day-19/**'

jobs:
test:
    runs-on: ubuntu-latest
    container:
    image: jonasjso/adventofcode2020
    steps:
      - uses: actions/checkout@v2
      - name: make test
        run: make test DAY=day-19

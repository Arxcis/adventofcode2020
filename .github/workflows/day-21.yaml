name: day-21
on:
  workflow_dispatch:

  push:
    branches:
      - main
    paths:
      - 'days/day-21/**'
    paths-ignore:
      - 'days/day-21/README.md'

  pull_request:
    branches:
      - main
    paths:
      - 'days/day-21/**'
    paths-ignore:
      - 'days/day-21/README.md'

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: 
    steps:
      - uses: actions/checkout@v2
      - name: make versions
        run: make versions
      - name: make test
        run: make test DAY=day-21

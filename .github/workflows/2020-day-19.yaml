name: 2020-day-19
on:
  workflow_dispatch:

  push:
    branches:
      - main
    paths:
      - '2020/day-19/test.sh'
      - '2020/day-19/io/**'
      - '2020/day-19/solutions/**'

  pull_request:
    branches:
      - main
    paths:
      - '2020/day-19/test.sh'
      - '2020/day-19/io/**'
      - '2020/day-19/solutions/**'

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: 
    steps:
      - uses: actions/checkout@v2
      - name: make versions
        run: make versions
      - name: make test
        run: make 2020.day-19

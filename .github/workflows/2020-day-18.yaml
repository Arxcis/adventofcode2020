name: 2020-day-18
on:
  workflow_dispatch:

  push:
    branches:
      - main
    paths:
      - '2020/day-18/test.sh'
      - '2020/day-18/io/**'
      - '2020/day-18/solutions/**'

  pull_request:
    branches:
      - main
    paths:
      - '2020/day-18/test.sh'
      - '2020/day-18/io/**'
      - '2020/day-18/solutions/**'

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: 
    steps:
      - uses: actions/checkout@v2
      - name: make versions
        run: make versions
      - name: make test
        run: make 2020.day-18

import fileinput; from functools import reduce; from operator import mul; [(print(sums[1]), print(reduce(mul, sums, 1))) for sums in [[sum((1 - (y % d)) * (l[((y // d) * r) % len(l)] == '#') for y, l in enumerate([line[:-1] for line in fileinput.input()])) for r, d in [(1, 1), (3, 1), (5, 1), (7, 1), (1, 2)]]]]
